[package]
name = "rhizome-pith-cron-portable"
description = "Portable cron expression implementation (works on native and WASM)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rhizome-pith-cron = { path = "../../../interfaces/pith-cron" }
